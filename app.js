const express = require("express");

const PORT = 3000;
const app = express();

const newData = "";
const newData2 = "";
const newData3 = "";
const newData4 = "";

app.listen(PORT, () => {
  console.log(`App is running on http://localhost:${PORT}`);
});
